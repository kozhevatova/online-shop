import React from 'react';
import './SearchBar.css';

const SearchBar = () => {
    return(
        <section className="search">
            <h2>SearchBar</h2>
        </section>
    );
}

export default SearchBar;